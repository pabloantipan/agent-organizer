build/bin
node_modules
frontend/dist
# git worktrees for builder sessions (supervise skill)
.wt/
# personal state of this checkout (cards, machine names, project ids)
working-on/
# ...except the scan fixtures, which are the test suite's, not this checkout's
!testdata/home/**/working-on/
